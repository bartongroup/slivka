options:
  - name: alpha
    label: Alpha
    parameter: --alpha ${value}
    value:
      type: integer
      required: yes

  - name: beta
    label: Beta
    parameter: --beta ${value}
    value:
      type: integer
      required: no
